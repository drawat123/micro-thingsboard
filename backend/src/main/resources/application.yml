mqtt:
  broker:
    url: "tcp://localhost:1883"
    clientId: "backend-01"
    topic: "telemetry/#"

logging:
  level:
    root: INFO
    com.divyam.telemetry: INFO

kafka:
  bootstrap-servers: "localhost:9092"
  topic: "raw-telemetry"
  acks: "1"